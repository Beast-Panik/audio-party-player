# Sperrt diesen Ordner komplett fuer direkte Web-Zugriffe (Apache 2.2 + 2.4)
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>
